{
  "name": "open-working-hours-e2e",
  "version": "1.0.0",
  "description": "E2E tests for Open Working Hours mobile app",
  "scripts": {
    "test": "jest",
    "test:ios": "PLATFORM=ios jest",
    "test:android": "PLATFORM=android jest",
    "test:auth": "jest flows/auth.test.js",
    "test:location": "jest flows/location.test.js",
    "test:calendar": "jest flows/calendar.test.js",
    "appium:start": "appium --allow-cors --relaxed-security",
    "appium:install-drivers": "appium driver install xcuitest && appium driver install uiautomator2"
  },
  "dependencies": {
    "webdriverio": "^9.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=20.19.0 <23.0.0 || >=24.0.0"
  }
}
